package com.semih.repository;

import com.semih.model.CashAid;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CashAidRepository extends JpaRepository<CashAid, Long> {
}
